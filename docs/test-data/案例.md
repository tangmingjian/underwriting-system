特征取数方法com.insurance.uw.interfaces.controller.FeatureExtractionController.extract
如果某个特征配置了AggregationLevel=ORDER ，StorageLevel=INSURED，一个订单下多个投保单，被保人夸这些投保单，有个投保单下的被保人1需要这个特征，有的不需要，需要避免不必要的计算
计算类型是api的特征可以在grovvy脚本buildRequest的方法里过滤不需要的客户，如果计算类型是入参类型的该如何做到