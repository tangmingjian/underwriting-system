重写特征取数方法里的特征取值和存储逻辑
特征取值接口：com.insurance.uw.interfaces.controller.FeatureExtractionController.extract
特征取值入参：
```java
    /** 订单（已可序列化） */
    private Order order;

    /** 保单 → 被保人 → 需要的特征码集合（按保单隔离） */
    private Map<String, Map<String, Set<String>>> policyInsuredFeatureMap;

    /** 保单 → 投保人 → 需要的特征码集合（按保单隔离） */
    private Map<String, Map<String, Set<String>>> policyApplicantFeatureMap;

    /** 被保人 → 同人客户号（for 同人查询） */
    private Map<String, Set<String>> customerNos;
```

AggregationLevel × StorageLevel 组合及典型场景（仅有以下9种）：
| 聚合级别 | 存储级别 | 典型场景 | 说明 |
|---|---|---|---|
| `ORDER` | `ORDER` | 订单反欺诈评分、订单总风险等级 | 全订单一次查询，结果存订单 |
| `ORDER` | `POLICY` | 从订单维度取各保单投保金额 | 遍历各保单分别存储 |
| `ORDER` | `APPLICANT` | 订单级查询投保人数据 | 遍历各投保人分别存储 |
| `ORDER` | `INSURED` | **信用评分（最常用）**：订单下批量查所有被保人 | 一次 HTTP，结果按人分发 |
| `POLICY` | `POLICY` | 产品保额上限查询 | 每保单一次查询 |
| `POLICY` | `APPLICANT` | 投保人收入核验 | 每保单查投保人 |
| `POLICY` | `INSURED` | **职业风险、入参取数**：保单下遍历被保人 | 每保单一次（或每保单每被保人） |
| `INSURED` | `INSURED` | **健康评分**：每人独立调单被保人接口 | 每个被保人独立 Future 并行 |
| `APPLICANT` | `APPLICANT` | 投保人独立征信查询 | 每个投保人独立并行 |

**选择原则**：
- **聚合级别**取决于"一次请求能覆盖多少人"：
    - 一次请求能查全订单所有人 → `ORDER`
    - 一次请求只能查一个保单下的人 → `POLICY`
    - 一次请求只能查一个人 → `INSURED` / `APPLICANT`
- **存储级别**取决于"结果给谁用"：
    - 订单级规则用 → `ORDER`
    - 被保人级规则用 → `INSURED`
    - 跨级规则也可以通过 `FeatureCollector` 的合并策略读取上级特征

特征取数注意点：
1. 如果某个特征配置了高层取数，下层存储
例如：AggregationLevel=ORDER ，StorageLevel=INSURED，
    正常逻辑：取订单下所有的被保人一次性执行取数逻辑，结果Map<被保人Id,特征值>，存储时按照被保人Id把结果分发到每个被保人的特征缓存里
    特殊逻辑：a.虽然特征配置了AggregationLevel=ORDER 一个订单下多个投保单多个被保人， 但是有的被保人不需要某个特征，计算该特征时需要过滤调不需要这个特征的人
            b.同一个被保人跨这些投保单存在，投保单1的被保人1需要这个特征，投保单2的被保人1不需要这个特征
            c.有可能同一个投保人或者被保人同特征在不同的投保单下特征值不一样的情况
2. 同被保人或者投保人在不同投保单下需要计算的特征不一定一样
3. 不要做不必要的计算，存储时要精确存储避免出现覆盖的情况
4. 注意不同计算类型的差异
5. 其他点你来补充


要求：
1. 逻辑正确 无bug
2. 代码清晰 可读性强 扩展性好
3. 足够抽象 


